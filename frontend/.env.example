# واجهة الـ API (اختياري)
#
# إذا تُرك فارغاً، تُرسل الطلبات إلى نفس المنشأ (مثلاً http://localhost:5173)
# ويعمل خادم التطوير (Vite) على تمرير أي طلب يبدأ بـ /api إلى الخلفية تلقائياً.
#
# عند النشر المنفصل (لا يوجد proxy) عيّن العنوان المطلق للخلفية:
VITE_API_URL=http://localhost:3000
